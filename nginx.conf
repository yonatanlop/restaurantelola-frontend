server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Proxy transparente al backend, dentro de la red Docker del compose
    location /api/ {
        proxy_pass http://backend:8080/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # SPA: cualquier ruta que no sea un archivo real cae a index.html (React Router)
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cache largo para assets versionados por Vite (hash en el nombre de archivo)
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
